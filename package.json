{
  "name": "dota-chop-shop",
  "version": "1.0.0",
  "description": "Calculate the most cost-efficient Dota 2 items based on stat values",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "dota-chop": "src/cli/index.ts"
  },
  "scripts": {
    "start": "bun run src/cli/index.ts",
    "dev": "bun run --watch src/cli/index.ts",
    "cli": "bun run src/cli/index.ts",
    "all": "bun run src/cli/index.ts all",
    "transitions": "bun run src/cli/index.ts transitions",
    "efficiency": "bun run src/cli/index.ts efficiency",
    "pathfind": "bun run src/cli/index.ts pathfind",
    "sequences": "bun run src/cli/index.ts sequences",
    "reachability": "bun run src/cli/index.ts reachability",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "bun x tsc --noEmit"
  },
  "keywords": [
    "dota2",
    "items",
    "efficiency",
    "calculator"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/cli-progress": "^3.11.6",
    "bun-types": "^1.3.4",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "cli-progress": "^3.12.0",
    "commander": "^14.0.2",
    "es-toolkit": "^1.43.0"
  }
}
